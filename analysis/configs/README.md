# Analysis Configs

This folder is for shared analysis configuration files.

Examples:

```text
common bin definitions
shared run lists
shared plotting settings
common input/output path templates
```

Channel-specific configs may also be placed inside the relevant channel folder when that is cleaner.
